<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyerp.dao.OrderMapper">
    <resultMap id="BaseResultMap" type="com.hyerp.model.Order">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="order_no" jdbcType="VARCHAR" property="orderNo" />
        <result column="customer_name" jdbcType="VARCHAR" property="customerName" />
        <result column="customer_phone" jdbcType="VARCHAR" property="customerPhone" />
        <result column="customer_address" jdbcType="VARCHAR" property="customerAddress" />
        <result column="total_amount" jdbcType="DECIMAL" property="totalAmount" />
        <result column="status" jdbcType="INTEGER" property="status" />
        <result column="order_date" jdbcType="TIMESTAMP" property="orderDate" />
        <result column="delivery_date" jdbcType="TIMESTAMP" property="deliveryDate" />
        <result column="remark" jdbcType="VARCHAR" property="remark" />
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
    </resultMap>

    <resultMap id="OrderReservedItemMap" type="com.hyerp.model.dtpojo.ReservedItemPojo">
        <result column="order_id" jdbcType="BIGINT" property="orderId" />
        <result column="item_id" jdbcType="BIGINT" property="itemId" />
        <result column="goods_id" jdbcType="BIGINT" property="goodsId" />
        <result column="order_quantity" jdbcType="INTEGER" property="orderQuantity" />
        <result column="reserved_id" jdbcType="BIGINT" property="reservedId" />
        <result column="inventory_id" jdbcType="BIGINT" property="inventoryId" />
        <result column="reserved_quantity" jdbcType="INTEGER" property="reservedQuantity" />
        <result column="needed_quantity" jdbcType="INTEGER" property="neededQuantity" />
    </resultMap>
    
    <sql id="BaseColumnList">
        `id`, `order_no`, `customer_name`, `customer_phone`, `customer_address`, 
        `total_amount`, `status`, `order_date`, `delivery_date`, `remark`, 
        `create_time`, `update_time`
    </sql>

    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="com.hyerp.model.Order" useGeneratedKeys="true">
        INSERT INTO `order` (
            `order_no`, `customer_name`, `customer_phone`, `customer_address`, 
            `total_amount`, `status`, `order_date`, `delivery_date`, `remark`, 
            `create_time`, `update_time`
        ) VALUES (
            #{orderNo}, #{customerName}, #{customerPhone}, #{customerAddress},
            #{totalAmount}, #{status}, #{orderDate}, #{deliveryDate}, #{remark},
            #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updateByPrimaryKey" parameterType="com.hyerp.model.Order">
        UPDATE `order`
        SET 
            `order_no` = #{orderNo},
            `customer_name` = #{customerName},
            `customer_phone` = #{customerPhone},
            `customer_address` = #{customerAddress},
            `total_amount` = #{totalAmount},
            `status` = #{status},
            `order_date` = #{orderDate},
            `delivery_date` = #{deliveryDate},
            `remark` = #{remark},
            `update_time` = #{updateTime}
        WHERE `id` = #{id}
    </update>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        DELETE FROM `order` WHERE `id` = #{id}
    </delete>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Long">
        SELECT
        <include refid="BaseColumnList"/>
        FROM `order`
        WHERE `id` = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="BaseColumnList"/>
        FROM `order`
        ORDER BY `create_time` DESC
    </select>

    <select id="selectNeedReserved" resultMap="BaseResultMap">
        SELECT
        <include refid="BaseColumnList"/>
        FROM `order`
        where `STATUS` = '1'
        ORDER BY `create_time` DESC
    </select>

    <select id="selectByOrderNo" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT
        <include refid="BaseColumnList"/>
        FROM `order`
        WHERE `order_no` = #{orderNo}
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        SELECT
        <include refid="BaseColumnList"/>
        FROM `order`
        WHERE `status` = #{status}
        ORDER BY `create_time` DESC
    </select>

    <select id="selectOrderReservedItem" resultMap="OrderReservedItemMap" parameterType="java.lang.Long">
        SELECT
            o.id as order_id,
            oi.id as item_id,
            oi.goods_id,
            oi.quantity as order_quantity,
            oir.id as reserved_id,
            oir.inventory_id,
            oir.reserved_quantity,
            oir.needed_quantity
        from `order` o
            INNER join order_item oi on o.id = oi.order_id
            LEFT JOIN order_item_reserved oir on oi.id = oir.item_id
        WHERE
            o.id = #{orderId}
    </select>
</mapper>

