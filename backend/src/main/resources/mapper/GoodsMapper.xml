<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyerp.dao.GoodsMapper">
    <resultMap id="BaseResultMap" type="com.hyerp.model.Goods">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="goods_code" jdbcType="VARCHAR" property="goodsCode" />
        <result column="goods_name" jdbcType="VARCHAR" property="goodsName" />
        <result column="description" jdbcType="VARCHAR" property="description" />
        <result column="price" jdbcType="DECIMAL" property="price" />
        <result column="unit" jdbcType="VARCHAR" property="unit" />
        <result column="category_id" jdbcType="BIGINT" property="categoryId" />
        <result column="status" jdbcType="INTEGER" property="status" />
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
    </resultMap>
    
    <sql id="BaseColumnList">
        `id`, `goods_code`, `goods_name`, `description`, `price`, `unit`, `category_id`, 
        `status`, `create_time`, `update_time`
    </sql>

    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="com.hyerp.model.Goods" useGeneratedKeys="true">
        INSERT INTO goods (
            `goods_code`, `goods_name`, `description`, `price`, `unit`, `category_id`, 
            `status`, `create_time`, `update_time`
        ) VALUES (
            #{goodsCode}, #{goodsName}, #{description}, #{price}, #{unit}, #{categoryId},
            #{status}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updateByPrimaryKey" parameterType="com.hyerp.model.Goods">
        UPDATE goods
        SET 
            `goods_code` = #{goodsCode},
            `goods_name` = #{goodsName},
            `description` = #{description},
            `price` = #{price},
            `unit` = #{unit},
            `category_id` = #{categoryId},
            `status` = #{status},
            `update_time` = #{updateTime}
        WHERE `id` = #{id}
    </update>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        DELETE FROM goods WHERE `id` = #{id}
    </delete>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Long">
        SELECT
        <include refid="BaseColumnList"/>
        FROM goods
        WHERE `id` = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="BaseColumnList"/>
        FROM goods
        ORDER BY `create_time` DESC
    </select>

    <select id="searchByName" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT
        <include refid="BaseColumnList"/>
        FROM goods
        WHERE `goods_name` LIKE CONCAT('%', #{name}, '%')
        ORDER BY `create_time` DESC
    </select>

    <select id="selectByGoodsCode" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT
        <include refid="BaseColumnList"/>
        FROM goods
        WHERE `goods_code` = #{goodsCode}
    </select>
</mapper>

